@model coreModel.Model.Unvanlar

@{
    ViewData["Title"] = "Sil";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div style="margin-left:500px;">
    <h1>Sil</h1>


    <div>
        <h4>Unvanlar</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.unvanAdi)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.unvanAdi)
            </dd>
        </dl>

        <form asp-action="Sil">
            <input type="hidden" asp-for="unvanID" />
            <input type="submit" value="Sil" class="btn btn-danger" style="margin-top:5px;" /> |
            <a asp-action="Liste">Geri dön</a>
        </form>
    </div>
</div>
